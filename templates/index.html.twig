{% extends "base.html.twig" %}

{% block body %}
    {{ form_start(form, {'action': path('process', {questionId: question.id}), 'method': 'POST'}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}